@model IEnumerable<HospitalApp.Models.Doctor>

<h1>Update Doctors</h1>

<ul>
    @foreach (var doctor in Model)
    {
        <li>
            <a asp-controller="Admin" asp-action="EditDoctor" asp-route-id="@doctor.Id">
                @doctor.Name
            </a>
        </li>
    }
</ul>