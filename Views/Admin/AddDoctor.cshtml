@model Doctor

<form asp-controller="Admin" asp-action="AddDoctor" method="post">
    <div class="mb-3">
        <label asp-for="Name" class="form-label"> Name</label>
        <input asp-for="Name" class="form-control">
    </div>

    <div class="mb-3">
        <label asp-for="ClinicId" class="form-label"> Clinic Id</label>
        <input asp-for="ClinicId" class="form-control">
    </div>


    

    <button type="submit" class="btn btn-primary">Save changes</button>
</form>

@* @model HospitalApp.Models.Doctor

@{
    ViewData["Title"] = "Add Doctor";
}

<h1>@ViewData["Title"]</h1>

<form method="post" asp-controller="Doctor" asp-action="Create">
    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    @if (ViewData["Clinics"] == null || !(ViewData["Clinics"] is SelectList))
{
    <p style="color:red;">Clinic list is empty or not set.</p>
}

    <div class="form-group">
        <label for="ClinicId">Clinic</label>
        <select asp-for="ClinicId" class="form-control">
            <option value="">Select a Clinic</option>
            @foreach (var clinic in (SelectList)ViewData["Clinics"])
            {
                <option value="@clinic.Value">@clinic.Text</option>
            }
        </select>
        <span asp-validation-for="ClinicId" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Add Doctor</button>
</form> *@